services:
  bot:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./configs:/app/configs:ro
      - ~/.aws:/home/appuser/.aws:ro
    environment:
      - AWS_PROFILE=${AWS_PROFILE:-default}
